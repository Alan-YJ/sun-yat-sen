export default class Venue{
    constructor({site_no,title,desc,pic,price,state,direction}){
        this.site_no = site_no,
        this.title = title,
        this.desc = desc,
        this.pic = pic,
        this.price = price,
        this.state = state,
        this.direction = direction
    }
}